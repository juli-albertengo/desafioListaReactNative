import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    text: {
        fontFamily: 'Montserrat-Bold',
        fontSize: 18,
        textAlign: 'center',
        margin: 10,
        color: '#000000',
    },
});
