import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    textInput: {
        borderColor: 'black',
        borderBottomWidth: 1,
        marginBottom: 10,
        width: '70%',
        height: 40,
        fontSize: 16,
        color: '#a4133c',
        fontWeight: 'bold',
      },
});
