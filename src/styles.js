import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    containerInput: {padding: 25,
        flexDirection: 'row',
        justifyContent: 'space-between',
        alignItems: 'center',
    },
    containerList: {
        paddingHorizontal: 25,
    },
    textList: {
        fontSize: 14,
        color: '#000000',
        marginBottom: 10,
        fontWeight: 'bold',
    },
});
